apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: foo
spec:
  dnsNames:
  - foo.example.com
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: production
  secretName: foo
